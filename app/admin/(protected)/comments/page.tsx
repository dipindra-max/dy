import {prisma} from '@/lib/prisma';import {moderateComment} from '../actions';export default async function Comments(){const comments=await prisma.comment.findMany({include:{post:true},orderBy:{createdAt:'desc'}});return <><div className="eyebrow">Moderation</div><h1>Comments</h1><div className="grid">{comments.length?comments.map(c=><div className="card" key={c.id}><div style={{display:'flex',justifyContent:'space-between',gap:15}}><strong>{c.name}</strong><span className="pill">{c.status}</span></div><p>{c.content}</p><p className="muted">On: {c.post.title} • {new Date(c.createdAt).toLocaleString()}</p><form className="socials" action={moderateComment}><input type="hidden" name="id" value={c.id}/><select className="select" name="status" defaultValue={c.status}><option>PENDING</option><option>APPROVED</option><option>REJECTED</option><option>SPAM</option></select><button className="btn primary">Update</button></form></div>):<div className="empty">No comments.</div>}</div></>}
