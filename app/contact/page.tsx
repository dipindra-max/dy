import {prisma} from '@/lib/prisma';import ContactForm from './form';export const metadata={title:'Contact'};
export default async function Contact(){const s=await prisma.siteSettings.findUnique({where:{id:'main'}});return <section className="section"><div className="container" style={{maxWidth:900}}><div className="eyebrow">Contact</div><h1 className="title" style={{fontSize:'3.6rem'}}>Let's talk.</h1><p className="subtitle">Questions, project ideas or feedback? Send a message.</p><div className="grid" style={{gridTemplateColumns:'1fr 1fr',marginTop:30}}><div className="card"><h3>Direct contact</h3><p><strong>Email</strong><br/><a href={`mailto:${s?.email}`}>{s?.email}</a></p><p><strong>Phone</strong><br/>{s?.phone}</p><p><strong>WhatsApp</strong><br/><a href={`https://wa.me/977${s?.whatsapp}`}>Chat on WhatsApp</a></p></div><div className="card"><ContactForm/></div></div></div></section>}
