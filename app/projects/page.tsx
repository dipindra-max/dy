import {prisma} from '@/lib/prisma';export const revalidate=60;export const metadata={title:'Projects'};
export default async function Projects(){const projects=await prisma.project.findMany({where:{status:'PUBLISHED'},orderBy:[{isFeatured:'desc'},{sortOrder:'asc'},{createdAt:'desc'}]});return <section className="section"><div className="container"><div className="eyebrow">Work</div><h1 className="title" style={{fontSize:'3.5rem'}}>Projects</h1><p className="subtitle">Projects are managed from the private admin dashboard.</p><div className="grid cards" style={{marginTop:30}}>{projects.length?projects.map(p=><article className="card" key={p.id}><span className="pill">{p.isFeatured?'Featured':'Project'}</span><h2>{p.title}</h2><p className="muted">{p.description}</p><div className="taglist">{p.technologies.map(t=><span className="pill" key={t}>{t}</span>)}</div><div className="socials" style={{marginTop:20}}>{p.demoUrl&&<a className="btn" href={p.demoUrl} target="_blank" rel="noreferrer">Live demo</a>}{p.githubUrl&&<a className="btn" href={p.githubUrl} target="_blank" rel="noreferrer">GitHub</a>}</div></article>):<div className="empty" style={{gridColumn:'1/-1'}}>No projects published yet.</div>}</div></div></section>}
