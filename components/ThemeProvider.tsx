'use client';
import {useEffect,useState} from 'react';
export default function ThemeProvider({children}:{children:React.ReactNode}){const [dark,setDark]=useState(false);useEffect(()=>{const saved=localStorage.getItem('dy-theme');const d=saved?saved==='dark':matchMedia('(prefers-color-scheme: dark)').matches;setDark(d);document.documentElement.classList.toggle('dark',d)},[]);function toggle(){const d=!dark;setDark(d);document.documentElement.classList.toggle('dark',d);localStorage.setItem('dy-theme',d?'dark':'light')}return <>{children}<button aria-label="Toggle theme" onClick={toggle} style={{position:'fixed',right:16,bottom:16,zIndex:50,border:'1px solid var(--line)',background:'var(--surface)',color:'var(--text)',borderRadius:999,padding:'10px 13px',boxShadow:'var(--shadow)',cursor:'pointer'}}>{dark?'☀️':'🌙'}</button></>}
