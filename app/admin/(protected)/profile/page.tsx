import {requireAdmin} from '@/lib/auth';export default async function Profile(){const u=await requireAdmin();return <><div className="eyebrow">Account</div><h1>Admin profile</h1><div className="card"><p><strong>Name:</strong> {u.name}</p><p><strong>Email:</strong> {u.email}</p><p><strong>Role:</strong> {u.role}</p><p className="muted">Password changes can be added later through a dedicated authenticated flow. The seed password is never stored in plaintext.</p></div></>}
