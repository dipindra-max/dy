'use client';import {useState} from 'react';export default function ContactForm(){const [state,setState]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('Sending…');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();setState(j.message||j.error);if(r.ok)e.currentTarget.reset()}return <form className="form" onSubmit={submit}><label className="label">Name<input className="input" name="name" required/></label><label className="label">Email<input className="input" type="email" name="email" required/></label><label className="label">Subject<input className="input" name="subject" required/></label><label className="label">Phone (optional)<input className="input" name="phone"/></label><label className="label">Message<textarea className="textarea" name="message" required/></label><button className="btn primary">Send message</button>{state&&<div className={state==='Sending…'?'':'notice'}>{state}</div>}</form>}
