import {prisma} from '@/lib/prisma';import {addCategory,deleteCategory} from '../actions';export default async function Categories(){const cats=await prisma.category.findMany({include:{_count:{select:{posts:true}}},orderBy:{name:'asc'}});return <><div className="eyebrow">Taxonomy</div><h1>Categories</h1><div className="card"><form className="searchbar" action={addCategory}><input className="input" name="name" placeholder="New category" required/><button className="btn primary">Add</button></form></div><div className="card" style={{marginTop:20}}>{cats.map(c=><div key={c.id} style={{display:'flex',justifyContent:'space-between',alignItems:'center',borderBottom:'1px solid var(--line)',padding:'12px 0'}}><span><strong>{c.name}</strong> <span className="muted">{c._count.posts} posts</span></span><form action={deleteCategory}><input type="hidden" name="id" value={c.id}/><button className="btn danger">Delete</button></form></div>)}</div></>}
