import {prisma} from '@/lib/prisma';import {SITE_URL} from '@/lib/seo';export default async function SEO(){const s=await prisma.siteSetting.findUnique({where:{id:'main'}});return <><div className="eyebrow">Search visibility</div><h1>SEO & monetization</h1><div className="grid"><div className="card"><h3>Site URL</h3><p>{SITE_URL}</p><h3>Search Console</h3><p>{s?.googleVerification?'Verification configured.':'Add your verification token in Settings.'}</p><h3>Analytics</h3><p>{s?.googleAnalyticsId?'Analytics ID configured.':'Add your GA ID in Settings.'}</p></div><div className="card"><h3>AdSense</h3><p>{s?.adsensePublisherId?'Publisher ID configured.':'Add your publisher ID after Google approves the site.'}</p><p className="muted">Keep ads clearly separated from navigation and content. Never incentivize or automate ad clicks.</p></div></div></>}
