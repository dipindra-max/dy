import Link from 'next/link';
export default function ArticleCard({post}:{post:any}){return <article className="card" style={{padding:0,overflow:'hidden'}}><div style={{height:190,background:'var(--surface2)'}}>{post.featuredImageUrl?<img src={post.featuredImageUrl} alt="" style={{width:'100%',height:'100%',objectFit:'cover'}}/>:null}</div><div style={{padding:22}}><div className="taglist"><span className="pill">{post.category.name}</span><span className="muted">{post.readingTime} min read</span></div><h3 style={{fontSize:'1.25rem',lineHeight:1.25}}><Link href={`/blog/${post.slug}`}>{post.title}</Link></h3><p className="muted">{post.excerpt || post.content.slice(0,130)+'…'}</p><small className="muted">{post.publishedAt?new Date(post.publishedAt).toLocaleDateString():'Draft'}</small></div></article>}
