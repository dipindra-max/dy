import {prisma} from '@/lib/prisma';import Link from 'next/link';import {deletePost} from '../actions';export default async function Posts(){const posts=await prisma.post.findMany({include:{category:true},orderBy:{createdAt:'desc'}});return <><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div className="eyebrow">Content</div><h1>Posts</h1></div><Link className="btn primary" href="/admin/posts/new">+ New post</Link></div><div className="card" style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr><th align="left">Title</th><th>Status</th><th>Category</th><th>Views</th><th>Actions</th></tr></thead><tbody>{posts.map(p=><tr key={p.id} style={{borderTop:'1px solid var(--line)'}}><td style={{padding:'14px 8px'}}>{p.title}</td><td>{p.status}</td><td>{p.category.name}</td><td>{p.viewCount}</td><td><div className="socials"><Link className="btn" href={`/admin/posts/${p.id}`}>Edit</Link><form action={deletePost}><input type="hidden" name="id" value={p.id}/><button className="btn danger">Delete</button></form></div></td></tr>)}</tbody></table></div></>}
