import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {contactSchema} from '@/lib/validators';import {sendContactNotification} from '@/lib/email';export async function POST(req:Request){try{const parsed=contactSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Please complete all fields correctly.'},{status:400});await prisma.message.create({data:parsed.data});await sendContactNotification(parsed.data);return NextResponse.json({message:'Thanks! Your message was received.'});}catch{return NextResponse.json({error:'Unable to send message right now.'},{status:500})}}
